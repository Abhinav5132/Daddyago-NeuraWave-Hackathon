## Settup python virtual environment
1. create a virtual environment: 
    ``` bash
    python3 -m venv .venv
    ```
2. set venv as the source:
    ``` bash 
    source .venv/bin/activate
    ```
3. install required python libraries: 
    ``` bash
    pip install pandas catboost numpy matplotlib
    ```